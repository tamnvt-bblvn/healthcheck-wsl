health_url: http://127.0.0.1:8010/health
interval_sec: 30
timeout_sec: 10
log_path: watcher.log
# Optional: Authorization: Bearer <token> if server enforces HEALTHCHECK_TOKEN
# bearer_token: your-secret
